a================================================================================
ANCORHASH · ETHROME 2026 · REPORT FASE 1 (Frontend & Swarm)
Data: 2026-09-11  ·  Branch: ethrome-2026
================================================================================

STATO: COMPLETATA. tsc, oxlint e vite build passano; dev server verificato.


--------------------------------------------------------------------------------
1. DOCUMENTI LETTI
--------------------------------------------------------------------------------

- docs/ETHROME_ARCHITECTURE.md (piano di build, 3 fasi)
- hacker-manual/ (submodule urbeETH/ethrome-2026-hacker-manual)
  Letti integralmente: CLAUDE.md, llms.txt, manual/prizes.md, manual/rules.md

Bounty target confermati dal manuale:
  Swarm      $1.000  · due vincitori da $500, nessuna categoria fissa
  Arkiv      $2.500  · Mission 02 "Built to expire" $500 + $1.000 Best Use
  Team1      $1.000  · Track B "Tokenized Assets" $300 (1°) / $100 (2°)


--------------------------------------------------------------------------------
2. ANALISI DEI FILE (pre-modifica)
--------------------------------------------------------------------------------

lib/hash.ts   12 righe. Solo sha256Hex(file) via window.crypto.subtle.
              Zero dipendenze, zero stato. Nessun contatto con la rete.

lib/api.ts    Client REST tipizzato. NotarizeRequest a 5 campi. Presenti
              extract() + ExtractResponse (Azure AI, invio file al backend).
              parseOrThrow<T> centralizza la gestione errori → ApiError.

App.tsx       648 righe, macchina a stati `Phase` a 6 varianti. Hash locale e
              OCR remoto lanciati in parallelo con Promise.all. Checkout
              simulato con timer, Turnstile monouso con reset nel finally.


--------------------------------------------------------------------------------
3. TRE DIVERGENZE DAL PIANO, RILEVATE E RISOLTE
--------------------------------------------------------------------------------

[A] "Sostituire SHA-256 con Swarm" avrebbe rotto la notarizzazione.

    src/Ancorhash.Core/Services/NotarizationService.cs:27-32 impone una regex
    stretta: document_hash deve essere SHA-256 hex di 64 caratteri. Una
    reference Swarm con encrypt:true è di 128 caratteri (32 byte indirizzo +
    32 byte chiave). Ogni notarizzazione avrebbe risposto 400.

    In più il brief Arkiv prescrive esattamente l'opposto (manual/prizes.md:42):
      "leave the file where it is and put an Arkiv entity beside it,
       a hash, a URL or a CID"

    DECISIONE: SHA-256 mantenuto come ancora di integrità; Swarm aggiunto
    come campo parallelo. Nessuna modifica al backend in Fase 1.

[B] Swarm ID non è una funzione pura: è un client con stato di connessione.

    API reale verificata sui typings v0.4.0 (non sulla documentazione):
      new SwarmIdClient({iframeOrigin, metadata, onConnectionChange})
      await client.initialize()      // monta un iframe nascosto
      await client.connect()         // passkey o account Ethereum
      await client.uploadData(bytes, {encrypt:true}) -> {reference}
      client.destroy()               // il client NON è riutilizzabile dopo

    Punto critico: ConnectionInfo.canUpload resta FALSE anche con identità
    connessa, se l'utente non ha postage stamp e non è configurato un gateway
    sovvenzionato. È lo stato più frequente prima di riscattare un gift code.

    DECISIONE: lo stato di connessione vive in App.tsx, non in hash.ts.

[C] expiration_days non permette di dimostrare Arkiv Mission 02.

    manual/prizes.md:68 — "Keep lifetimes at seconds or a couple of minutes,
    because nobody can wait on Sunday morning."

    Con la sola unità "giorni" nessun giudice vedrebbe mai un record scadere.

    DECISIONE: il campo inviato è expiration_seconds (unità esplicita nel
    nome), con input in giorni per il prodotto reale + preset demo da 60s.


--------------------------------------------------------------------------------
4. DECISIONI CONFERMATE DALL'UTENTE
--------------------------------------------------------------------------------

1. hash.ts      → mantenere SHA-256, aggiungere Swarm a fianco
2. Azure AI     → rimozione totale (anche extract() e ExtractResponse)
3. Scadenza     → giorni + preset demo, inviati come expiration_seconds
4. Install      → autorizzato
5. Fallback     → BLOCCANTE: niente reference Swarm, niente notarizzazione


--------------------------------------------------------------------------------
5. MODIFICHE APPLICATE
--------------------------------------------------------------------------------

NUOVO  src/Ancorhash.Web/src/lib/swarm.ts                          (+118)

  createSwarmClient(onConnectionChange)  factory del client
  uploadEncrypted(client, file, onProgress?) -> reference 128 hex
  uploadUnavailableReason(info) -> string|null   messaggi UI in italiano,
                                   distingue no-stamp da stamper-failed
  toPublicAddress(reference) -> primi 64 hex, chiave esclusa
  SwarmError                             errore di dominio

  Legge VITE_SWARM_ID_ORIGIN (default https://swarm-id.snaha.net) e
  l'opzionale VITE_SWARM_GATEWAY_URL.

MOD    src/Ancorhash.Web/src/lib/api.ts                            (+22/-36)

  NotarizeRequest += swarm_reference: string
                  += expiration_seconds: number
  RIMOSSI extract() e ExtractResponse.

MOD    src/Ancorhash.Web/src/App.tsx                          (+~370/-~110)

  + ciclo di vita Swarm ID in useEffect (client creato DENTRO l'effect:
    destroy() lo rende inutilizzabile e StrictMode rimonta due volte)
  + pannello "Archiviazione cifrata (Swarm)" con stato e connect
  + dropzone disabilitata finché canUpload è false
  + sezione "Validità del record": input giorni (1..3650) + checkbox demo
  + blocco "Documento Cifrato (Swarm)" con indirizzo pubblico E reference
    completa, con avviso che la reference è l'unico modo per rileggere
  + pendingFileRef + "Riprova upload" (il gift code si riscatta spesso
    DOPO il primo tentativo fallito)
  + progress % durante l'upload
  - rimossi isAiEnabled, tipo Extraction, blocco UI "Dati Estratti"
  ~ Phase: `extraction` sostituito da `swarmReference`
  ~ hash calcolato PRIMA dell'upload, non in parallelo: separa nettamente
    "file illeggibile" da "Swarm non disponibile"

MOD    src/Ancorhash.Web/.env

  + VITE_SWARM_ID_ORIGIN, + VITE_SWARM_GATEWAY_URL (commentato)
  NOTA: .env è in .gitignore (.gitignore:34). Non serve distribuirlo: il
  default è nel codice e l'app funziona senza.

MOD    package.json / package-lock.json     @snaha/swarm-id ^0.4.0, +35 pkg


--------------------------------------------------------------------------------
6. INVARIANTE DI SICUREZZA INTRODOTTA
--------------------------------------------------------------------------------

Con encrypt:true la reference Swarm È il segreto: 128 hex = indirizzo +
chiave di decifratura. Pubblicarla su un registro pubblico (Arkiv, on-chain)
equivarrebbe a pubblicare il documento in chiaro.

  reference completa (128 hex)  → resta SOLO nel browser, mostrata all'utente
  toPublicAddress() (64 hex)    → unica parte inviata al backend

Il flusso di notarizzazione invia toPublicAddress(swarmReference).
Questo va rispettato anche in Fase 2 (Arkiv) e Fase 3 (Avalanche).


--------------------------------------------------------------------------------
7. VERIFICA ESEGUITA
--------------------------------------------------------------------------------

  npx tsc -b --force            exit 0, nessun errore
  npx oxlint                    nessun finding
  npm run build                 build in 147ms, OK
  vite dev su :5199             index 200, App.tsx 200, swarm.ts 200
                                VITE_SWARM_ID_ORIGIN correttamente iniettata
  pre-bundle @snaha/swarm-id    200, ZERO import "node:" non risolti
                                (era il rischio principale: bee-js in browser)

NON verificato — richiede intervento umano e non è automatizzabile qui:
  - login con passkey su Swarm ID
  - upload reale su Swarm (serve un gift code, banco Swarm venerdì sera)
  - notarizzazione end-to-end (serve il backend su localhost:7071)


--------------------------------------------------------------------------------
8. PUNTI APERTI
--------------------------------------------------------------------------------

BUNDLE  dist/assets/index.js è 526,82 kB (164,30 kB gzip) e supera la soglia
        di warning di Vite; la crescita è attribuibile a bee-js dentro
        swarm-id. Non è un errore. Se serve, code-splitting in Fase 2.

AUDIT   4 vulnerabilità transitive: axios (SSRF, high) via @ethersphere/bee-js,
        nanoid (high) dall'albero Vite. Non raggiungibili dal path di upload
        su testnet. Sconsigliato forzare un downgrade di bee-js a metà
        hackathon.

BACKEND /api/v1/extract esiste ancora lato C#: ora non ha più chiamanti.
        Fuori dallo scope della Fase 1 (frontend); da decidere se rimuoverlo.

MEMORIA project_vision.md indica Azure come parte della visione strategica.
        La rimozione riguarda SOLO la UI di estrazione: Azure Functions
        resta il backend di tutta l'applicazione.


--------------------------------------------------------------------------------
9. AVVERTENZE DAL MANUALE — NON NEL PIANO DI BUILD
--------------------------------------------------------------------------------

[SUPERATO — vedi A1] La conversazione obbligatoria del sabato entro le
   20:00 (manual/prizes.md:86) NON è più un requisito. Confermato dal
   documento guides/ethrome-current del MCP Arkiv (maintained-2026-09-10),
   che elenca esplicitamente la "compulsory Saturday conversation" fra le
   regole da NON ripristinare. Resta utile parlare con il team Arkiv, ma
   non è una scadenza di qualificazione.

[AGGIORNATO — vedi A1] Due file nel repo:
   - arkiv/schema.md    NON è più un requisito di qualificazione: è
                        "useful project documentation... an optional
                        evidence index". Lo scriviamo comunque, come lavoro
                        di design e come indice delle evidenze.
   - arkiv/friction.md  bug report. NON vale un quinto: con la rubrica
                        attuale "Arkiv feedback" vale il 25%, cioè un
                        QUARTO. Scritto, 6 voci verificate.

!! PROGETTO PRE-ESISTENTE (manual/rules.md:50-52). Tre condizioni, tutte
   obbligatorie, pena esclusione da premio E bounty:
   1. dichiararlo esplicitamente nella submission
   2. solo la parte nuova viene valutata
   3. repo esistente reso pubblico, storia visibile, niente repo nuovo
   Stato: OK, si sta lavorando su ethrome-2026 nel repo esistente.
   DA FARE: rendere il repository pubblico.

!! Team1: UN SOLO track per progetto (manual/prizes.md:246). Track B richiede
   "an asset rule, an eligibility or transfer policy, and settlement or
   payment logic". Il solo mint previsto in Fase 3 non basta a qualificare.

!! Team1 richiede una SECONDA submission sul loro form, oltre alla spunta
   sul form ETHRome (manual/prizes.md:256).

!! Swarm: gift code al banco venerdì sera. Senza, canUpload resta false e —
   data la scelta di bloccare — l'intera demo non parte.

!! Deadline submission: DOMENICA 13 SETTEMBRE 10:00, nessuna proroga.
   Serve anche un video demo di massimo 3 minuti.


--------------------------------------------------------------------------------
10. PROSSIMO PASSO
--------------------------------------------------------------------------------

Fase 2 (Backend & Arkiv). Il contratto frontend è pronto e stabile:
il backend riceve già swarm_reference e expiration_seconds — System.Text.Json
ignora le proprietà sconosciute, quindi nulla si rompe finché il DTO C# non
viene esteso.

Da fare in Fase 2:
  1. NotarizeHttpRequest.cs  += SwarmReference, ExpirationSeconds
  2. entità Arkiv con scadenza = expiration_seconds (Mission 02)
  3. attributi tipizzati pensati per FILTRI COMPOSTI, non lookup per id:
     query depth vale 30 punti su 100 nel punteggio Arkiv
  4. il payload NON è interrogabile: la scelta attributi/payload va motivata
     nella submission, vale altri 20 punti

================================================================================


================================================================================
APPENDICE · SESSIONE 2 — GIFT CODE SWARM, MCP ARKIV, FRICTION REPORT
Data: 2026-09-11
================================================================================

--------------------------------------------------------------------------------
A1. CORREZIONE IMPORTANTE AL PUNTO 9 DI QUESTO REPORT
--------------------------------------------------------------------------------

Nella sezione 9 avevo indicato come scadenza dura la "conversazione di 10 minuti
con il team Arkiv entro sabato 20:00". NON È PIÙ VERO.

Fonte autorevole: MCP ufficiale Arkiv ETHRome, documento guides/ethrome-current
(maintained-2026-09-10), che dice testualmente:

  "Do not restore the older five requirements, compulsory Saturday
   conversation, three-feedback-bullet minimum, old deadlines or the
   previous five-part ranking."

Conseguenze concrete:

  1. La conversazione obbligatoria del sabato NON esiste più.
  2. Il punteggio a 5 voci del manuale (Query depth 30 / Evidence 20 /
     Fit 20 / Friction 20 / Craft 10) È SUPERATO.
  3. arkiv/schema.md NON è più un requisito di qualificazione: è
     "useful project documentation... an optional evidence index,
     not the sole entry route".

RUBRICA ATTUALE (dal Hub Arkiv e da guides/ethrome-current):

  30%  Why Arkiv / Web3 database?
  25%  Technical execution
  20%  Usefulness and adoption potential
  25%  Arkiv feedback     <-- il file friction/feedback SALE da 1/5 a 1/4

In sintesi: il manuale cartaceo ETHRome (synced 2026-09-10) è più vecchio delle
fonti live di Arkiv. Questo è esso stesso materiale da friction report (F-07).


--------------------------------------------------------------------------------
A2. GIFT CODE SWARM — DECODIFICATO E VERIFICATO ON-CHAIN
--------------------------------------------------------------------------------

QR decodificato con il framework Vision di macOS (swift, nessuna installazione).
Payload: {"v":1,"pk":"0x…","batch":"0x…"}

VERIFICA DI PROPRIETÀ:
  indirizzo derivato dalla chiave privata : 0x023d0149…0bb89c
  owner del batch on-chain                : 0x023d0149…0bb89c
  -> COINCIDONO: la chiave può firmare i postage stamp di quel batch.

STATO DEL BATCH (contratto 0x45a1502382541cd610cc9068e88727426b696293, Gnosis):
  depth 23 / bucketDepth 16   -> 32 GiB teorici, mutable
  residuo  +7.281.626.822     -> batch VIVO
  TTL      83.050 blocchi ~ 4,8 giorni  -> scade ~mercoledì 16/09

  Formula presa dal bundle di @snaha/swarm-id (funzione Aw), non inventata:
  (normalisedBalance - currentTotalOutPayment) / lastPrice * 5s

ESITO: gift code VALIDO e FINANZIATO. Margine ~3 giorni oltre la deadline.

RISCATTO: NON automatizzabile. Verificato che l'SDK @snaha/swarm-id non espone
alcuna API di riscatto (0 occorrenze di gift/redeem/importStamp/addStamp) e che
getPostageBatch() è di sola lettura; la documentazione swarm.snaha.net/docs non
menziona affatto i gift code. È un'operazione solo-UI dietro passkey, quindi un
passaggio umano. Aperto https://swarm-id.snaha.net nel browser.

DATI OPERATIVI: in key.txt (gitignorato, .gitignore:68). NON in questo file,
che è destinato al repo pubblico.


--------------------------------------------------------------------------------
A3. MCP ARKIV — TROVATO, COLLEGATO, INTERROGATO
--------------------------------------------------------------------------------

All'inizio NON era configurato (nessun .mcp.json, solo tavily in ~/.claude.json).
URL recuperato da hub.arkiv.network/ethrome, che risulta LIVE:

  https://arkiv-mcp-gateway.vercel.app/ethrome   (Streamable HTTP, nessuna chiave)

Aggiunto a scope progetto -> .mcp.json (nessun segreto, committabile).
Server: arkiv-ethrome v1.0.19. Espone 15 tool.

Tool utili per noi:
  network_status         bootstrap completo in una chiamata
  generate_entity_model  Postgres CREATE TABLE -> modello entità Arkiv
  check_schema           controlli deterministici su schema.md
  check_submission       controlli deterministici sulla submission
  search/read_knowledge  base di conoscenza ufficiale datata

RISPOSTA ALLA DOMANDA "ci sono comandi per produrre schema.md e friction.md":

  friction.md -> NO. Il tool prepare_feedback è citato nelle istruzioni di
                 initialize del server ma NON esiste in tools/list. Scritto
                 a mano. (È la voce F-01 del friction report.)

  schema.md   -> SÌ, ma NON utilizzabile ORA: generate_entity_model pretende
                 statement PostgreSQL CREATE TABLE. Ancorhash non ha Postgres
                 né un indexer, quindi non c'è nulla da convertire.
                 Corollario: Mission 01 (Decommission) è FUORI PORTATA, come
                 dice il manuale — "If you did not arrive with an indexer
                 already running, pick another mission".
                 Mission 02 resta il bersaglio giusto, ed è esattamente quella
                 per cui la Fase 1 ha già predisposto expiration_seconds.

DATI DI RETE PER LA FASE 2 (da network_status, probe fresco 2026-09-11):

  rete        : Tiramisu
  chainId     : 7738577
  RPC         : https://rpc.tiramisu.db-chain.testnet.arkiv.network
  WebSocket   : wss://rpc.tiramisu.db-chain.testnet.arkiv.network
  explorer    : https://tiramisu.explorer.arkiv.network
  faucet      : https://hub.arkiv.network/faucet      (sign-in + CAPTCHA, umano)
  access keys : https://hub.arkiv.network/access-keys (opzionali, alzano i rate limit)
  install     : npm install @arkiv-network/sdk@0.8.0 viem


--------------------------------------------------------------------------------
A4. NUOVO FILE: arkiv/friction.md
--------------------------------------------------------------------------------

7 voci, ognuna con il comando esatto per riprodurla. Nessuna inventata: tutte
verificate oggi. Vale il 25% del punteggio Arkiv.

  F-01  med   MCP annuncia prepare_feedback ma non lo espone (15 tool, assente)
  F-02  med   Tre fonti ufficiali, tre versioni SDK: 0.7.x / 0.8.0 / 0.8.1
  F-03  HIGH  arkiv-sdk e golem-base-sdk ancora pubblicati e NON deprecati su
              npm, con descrizioni che si dichiarano "part of the Arkiv project"
  F-04  HIGH  Il Data Explorer non offre Tiramisu (solo braga/kaolin); la pagina
              dati del block explorer è "EXPERIMENTAL — USE WITH CAUTION".
              Entrambe le superfici di ispezione pubbliche sono inagibili.
  F-05  low   EUR 2.500 sul Hub vs $2.500 USDC su Ethereum nel manuale
  F-06  med   La pagina Hub non contiene il template schema che il manuale dice
              essere lì
  F-07  HIGH  Due rubriche vive, e quella stampata è quella vecchia

Include anche una sezione "What worked well" (network_status come bootstrap in
una sola chiamata; il fatto che l'MCP dichiari i propri limiti con
verificationScope e tiramisuVerified:false invece di nasconderli). Un report di
sole lamentele risulta meno credibile di uno calibrato.

Restano 7 voci runtime da riempire costruendo la Fase 2: faucet, primo write,
query composta, comportamento della scadenza, websocket vs polling. Sono già
elencate come checklist in fondo al file.

NOTA SUL NOME: il manuale dice friction.md, le istruzioni dell'MCP dicono
feedback.md. Scelto friction.md in arkiv/. Rinominabile in un secondo.


--------------------------------------------------------------------------------
A5. FILE TOCCATI IN QUESTA SESSIONE
--------------------------------------------------------------------------------

NUOVO  arkiv/friction.md    report di friction, 7 voci verificate
NUOVO  .mcp.json            MCP Arkiv, scope progetto (nessun segreto)
(key.txt è stato creato e poi ELIMINATO dall'utente: il gift code è già
 stato riscattato e lo stamp è attivo. Resta in .gitignore per sicurezza.)
MOD    .gitignore           +key.txt, +*.key, +gift-code* (sezione segreti)
MOD    a.txt                questa appendice

Nessuna modifica al codice dell'applicazione in questa sessione.


--------------------------------------------------------------------------------
A6. PROSSIMO PASSO
--------------------------------------------------------------------------------

1. UMANO: riscattare il gift code su https://swarm-id.snaha.net (vedi key.txt).
   Verifica: nell'app il badge passa da "Non disponibile" a "Pronto".
2. Fase 2 su Tiramisu, catturando la friction mentre accade invece di
   ricostruirla dopo.
3. Rendere pubblico il repository (requisito di regolamento) — DOPO aver
   controllato che key.txt non sia tracciato: `git status --short | grep key`

================================================================================


================================================================================
APPENDICE B · SESSIONE 3 — STEP 0 (housekeeping) + STEP 1 (architettura)
Data: 2026-09-11
================================================================================

--------------------------------------------------------------------------------
B0. RETTIFICHE APPLICATE A QUESTO DOCUMENTO
--------------------------------------------------------------------------------

- Sezione 9: la "SCADENZA DURA" del sabato è marcata SUPERATA (vedi A1).
- Sezione 9: "friction vale un quinto" corretto -> vale il 25%, un QUARTO.
- Sezione 9: arkiv/schema.md non è più requisito di qualificazione; lo
  scriviamo comunque come lavoro di design e indice delle evidenze.
- A5: key.txt rimosso dall'elenco (eliminato dall'utente, gift code già
  riscattato, stamp attivo). Resta in .gitignore per sicurezza.


--------------------------------------------------------------------------------
B1. STEP 0a — F-01 RITIRATA: ERRORE NOSTRO
--------------------------------------------------------------------------------

F-01 sosteneva che il MCP annunciasse prepare_feedback senza esporlo.
ERA SBAGLIATA. prepare_feedback ESISTE: è un MCP **prompt**, non un tool.

  prompts/list -> 9 prompt:
    start, prepare_feedback, minimal_app, integrate_package, model_entities,
    diagnose_query, apply_skill, qualify, migrate

Stesso server, serverVersion 1.0.19, configRevision 2026-09-11.4: la STESSA
revisione sondata in sessione 2. Non è comparso fra le sessioni: c'era da
sempre. L'errore è stato chiamare solo tools/list e mai prompts/list, pur
avendo la capability "prompts" dichiarata nella risposta di initialize.

AZIONE: voce ELIMINATA, non ammorbidita. Le restanti sono state rinumerate
F-01..F-06. La ritrattazione è documentata nell'intestazione di friction.md.

LEZIONE OPERATIVA: un MCP espone tools + prompts + resources. Sondarli tutti
e tre prima di dichiarare assente qualcosa. In un file che vale il 25%, una
voce non verificata mina anche quelle corrette.

BONUS: lo stesso controllo ha rivelato 289 resources, fra cui
docs/querying, sdk-ethrome/query, sdk-ethrome/createentity,
official/typescript-sdk/mutating-data, official/json-rpc/mutating-entities.
Sono le fonti verificate che servono per lo STEP 2.


--------------------------------------------------------------------------------
B2. STEP 0c — SCANSIONE SEGRETI: PULITA (due scanner concordi)
--------------------------------------------------------------------------------

gitleaks 8.30.1, 21 commit, ~622 KB: 1 finding, FALSO POSITIVO.

  [generic-api-key] README.md:161  commit 4ca9f512  entropy 3.55
  Match: Evm__RelayerPrivateKey": "0xLA_TUA_CHIAVE_PRIVATA"

È un placeholder in italiano. Accanto, Turnstile__SecretKey contiene la
chiave di test pubblica di Cloudflare. Il commit sta sul branch
feature/azure-ai-water-sampling, non su main né su ethrome-2026.

La scansione nativa a pattern (492 oggetti) aveva già dato lo stesso esito:
solo placeholder 0xabcdef... nei test, hash di cache MSBuild, e un SHA-256
di esempio nel README. Nessun segreto reale nella history.

IGIENE (non segreti): bin/ e obj/ sono committati
(src/Ancorhash.Core/obj/..., src/Ancorhash.Infrastructure/bin/...).
Sporcano il diff che leggeranno i giudici.


--------------------------------------------------------------------------------
B3. STEP 0d — IL BRANCH NON HA COMMIT  [AZIONE RICHIESTA]
--------------------------------------------------------------------------------

  ethrome-2026 -> b83b45c  2026-08-05
  main         -> b83b45c  2026-08-05
  git rev-list --count main..ethrome-2026  ->  0

Tutta la Fase 1 è working tree non committato. Per manual/rules.md:52
contano solo i commit successivi all'inizio dell'hackathon: al momento il
lavoro conteggiabile è ZERO. L'utente ha scelto "Commit Phase 1 now" ma la
domanda è stata poi ritirata: NULLA È STATO COMMITTATO.

Tag proposto (il fork point coincide con il tip):
  git tag -a pre-ethrome-2026 b83b45c -m "Baseline before ETHRome 2026"
  git push origin pre-ethrome-2026
I giudici diffano pre-ethrome-2026..HEAD.

Repo: https://github.com/N0g4D/azure-web3-notarizer.git


--------------------------------------------------------------------------------
B4. FATTI ARKIV VERIFICATI (fonti citate, nulla inventato)
--------------------------------------------------------------------------------

Da official/typescript-sdk/mutating-data:

  Entità   = payload + contentType (MAI indicizzati, max 128 KiB)
           + attributes (max 30, tipizzati, indicizzati)
           + expires (altezza di BLOCCO, non secondi)
  engine   -> key (32 byte), owner, creator, createdAt, updatedAt, expiresAt
  Nomi attributo: 1-32 ASCII, minuscola iniziale, poi [a-z0-9._-], snake_case.
  Riservati: prefisso $, sequenza --, e le parole and/or/not/true/false/
  startswith/exists/typeof + i tag di tipo.

  TIPI E OPERATORI (da @arkiv-network/sdk/attr):
    str      <=128 byte UTF-8      eq, startsWith
    bool                           eq
    i32      -2^31 .. 2^31-1       eq, range
    u64      0 .. 2^64-1           eq, range
    u256     0 .. 2^256-1          eq, range
    dec      decimale <=18 cifre   eq, range
    addr     20 byte               eq
    key      32 byte entity key    eq
    bytes32  32 byte qualsiasi     eq

  Solo i quattro tipi numerici hanno indice ordinato. Un numero salvato come
  str supporta solo eq e prefisso. I valori vanno SEMPRE avvolti nel
  costruttore: eq("seller", str(...)) e eq("seller", addr(...)) sono domande
  diverse e solo quella col tipo giusto trova l'entità.

Da official/json-rpc/mutating-entities:

  Precompile Arkiv : 0x4400000000000000000000000000000000000044 (uguale su
                     ogni chain Arkiv)
  BLOCK TIME       : 2 SECONDI      <-- serve per expiration_seconds -> blocchi
  Transaction value: 0
  Calldata         : execute((uint8 operation, bytes operationData)[] ops)
                     selector 0x49650044, batch atomico e ordinato
  Tag operazioni   : 1 Create, 2 Patch, 3 Extend Expiry,
                     4 Transfer Ownership, 5 Delete
  Create struct    : (uint128 salt, uint64 expiresAt, uint64 minLifetime,
                      uint8 creationFlags, Attribute[] attributes)
                     expiresAt = blocco assoluto di scadenza

CONSEGUENZA DI PROGETTO: expiration_seconds deve essere convertito in
blocchi (secondi / 2) e sommato al blocco corrente per ottenere expiresAt.
Il preset demo da 60 s corrisponde a ~30 blocchi.


--------------------------------------------------------------------------------
B5. STEP 1 — DECISIONE DI ARCHITETTURA (PROPOSTA, in attesa di review)
--------------------------------------------------------------------------------

Opzioni valutate:

(a) C# puro via Nethereum
    FATTIBILE, e più di quanto sembrasse: il protocollo è documentato per
    intero (precompile, selector, struct). Nethereum sa mandare quella
    transazione.
    COSTO: va codificato a mano Attribute[] per tutti e nove i tipi e va
    reimplementato anche il lato query, senza rete di sicurezza. La pagina
    ufficiale stessa dice "Most developers should use the TypeScript SDK
    instead of raw transactions".
    RESA PER IL PUNTEGGIO: nessuna. Arkiv valuta "Why Arkiv" e "Technical
    execution", non la reimplementazione dell'SDK in C#.
    -> Scartata per rischio/tempo, non per impossibilità.

(b) Azure Function TypeScript per le sole scritture
    SDK ufficiale, rischio basso, modello gas-sponsored intatto.
    Ma anche le letture passerebbero dal backend: un hop in più, e la prova
    della Mission 02 (stessa query prima/dopo la scadenza) diventa goffa da
    mostrare.

(c) IBRIDO: scritture firmate dal relayer (TS) + query in sola lettura dal
    frontend React                                      <-- RACCOMANDATA
    - Scritture: SDK ufficiale, lato server, firmate dal relayer. Gas
      sponsorizzato, owner delle entità coerente, l'utente non ha bisogno di
      un wallet (coerente con l'attuale MOCK_WALLET_ADDRESS).
    - Letture: public client direttamente da React verso l'RPC Tiramisu.
      Nessun hop, e soprattutto la prova della Mission 02 gira DAL VIVO
      nella UI davanti ai giudici: è esattamente ciò che la mission chiede.
    - Predispone anche la Mission 03 (subscribe via websocket), se la
      vorremo.

    TRADE-OFF DA DICHIARARE, non da nascondere:
    - Due runtime: .NET per la notarizzazione, Node per Arkiv.
    - Owner delle entità = wallet del relayer. Quindi "org" è DICHIARATO,
      non dimostrato dalla firma. Va scritto nello schema.
    - L'access key NON va nel frontend: finirebbe nel bundle. Il frontend
      usa l'RPC pubblico rate-limited; l'access key resta lato backend.

Friction registrata: F-07 (nessun SDK .NET). Non è un bug, è un gap, ed è
quello che ha spostato l'architettura.


--------------------------------------------------------------------------------
B6. FILE TOCCATI IN QUESTA SESSIONE
--------------------------------------------------------------------------------

MOD    arkiv/friction.md   F-01 eliminata, voci rinumerate F-01..F-06,
                           ritrattazione in testa, aggiunta F-07 (.NET SDK)
MOD    a.txt               rettifiche sezione 9 e A5 + questa appendice B
NUOVO  (nessuno)
INSTALL gitleaks 8.30.1 via brew (autorizzato)

Nessuna modifica al codice dell'applicazione. Nessun pacchetto npm
installato: @arkiv-network/sdk@0.8.0 verrà installato allo STEP 4, previa
conferma.

================================================================================


================================================================================
APPENDICE C · STEP 2 (arkiv/schema.md) + STEP 2b (Why Arkiv)
Data: 2026-09-11
================================================================================

C0. VERIFICA GIT (richiesta dall'utente)
--------------------------------------------------------------------------------
  git log --oneline pre-ethrome-2026..ethrome-2026
  -> 7c85349 ETHRome Phase 1: encrypted Swarm storage with Swarm ID

  13 file, +1840/-167. Contiene swarm.ts, App.tsx, api.ts, friction.md,
  a.txt, .mcp.json, .gitignore, .gitmodules, gitlink hacker-manual.
  bin/obj tracciati: 0. obj/ ignorato (.gitignore:74).
  HEAD == origin/ethrome-2026 == 7c85349. Tag pre-ethrome-2026 -> b83b45c,
  pushato. CORRETTO.
  Nota minore: hacker-manual risulta untracked in status pur avendo gitlink e
  .gitmodules committati; un clone pulito richiede --recurse-submodules.


C1. STEP 2 — arkiv/schema.md
--------------------------------------------------------------------------------
Un solo entity type: notarization. 9 attributi su 30 disponibili.

  app             str      ancora di ogni query (serve >=1 filtro)
  type            str      lascia spazio ad altri record kind
  schema_version  i32      numerico per gte() in migrazione; str ordinerebbe
                           "10" prima di "9"
  document_hash   bytes32  SHA-256 E' esattamente 32 byte -> tipo nativo
  swarm_address   bytes32  indirizzo Swarm = 32 byte -> idem
  org             str      slug, eq + startsWith
  doc_type        str      slug, eq + startsWith
  chain_id        u64      i32 basterebbe oggi, ma i chain id EIP-155 sono
                           unsigned e possono superare 2^31
  notarized_at    u64      NON NEGOZIABILE: solo i 4 tipi numerici hanno
                           indice ordinato. Come str avrebbe solo eq e
                           startsWith -> muore ogni query a finestra
                           temporale. Date.now() non entra in i32.

SCELTA CONTROCORRENTE RISPETTO ALLA RICHIESTA: document_hash e swarm_address
sono bytes32, non str. Come str funzionerebbero (64 char < 128 byte) ma
darebbero solo eq/startsWith e occuperebbero 64 byte di indice invece di 32.
bytes32 dà solo eq, e non perde nulla: il prefisso di un digest non è una
domanda sensata. Se servisse la ricerca per hash breve si aggiungerà un
attributo separato document_hash_hex:str, senza degradare il primario.

PAYLOAD (mai indicizzato): tx_hash, anchor_chain, explorer_url, description.
Criterio: un attributo si guadagna il posto solo se compare in un where().

FUORI DA ARKIV: reference Swarm completa (128 hex = indirizzo + CHIAVE),
contenuto del file, nome del file, dati personali.

4 query composte scritte in sintassi verificata, tutte ancorate con
createdBy(RELAYER_ADDRESS): $creator è immutabile e non falsificabile, mentre
app:"ancorhash" è una stringa che chiunque può copiare sulle proprie entità.


C2. LA SCOPERTA CHE HA CAMBIATO IL DESIGN
--------------------------------------------------------------------------------
Prima bozza: ExpirationTime.fromSeconds(). SBAGLIATO per una demo osservata.

check_schema del MCP ha risposto:
  "Tiramisu makes no wall-clock promise. On 2026-09-04 the idle sample was
   ~20 seconds per block, while a loaded sample was ~2 seconds per block.
   That 10x range means fromMinutes(2) could have taken roughly 20 real
   minutes or roughly 2. For anything a judge has to watch, use
   ExpirationTime.fromBlocks(n), read expiresAt back from the receipt, and
   remeasure the current cadence before the demo."

MISURAZIONE NOSTRA (2026-09-11, head 326556, RPC Tiramisu):
  ultimi   50 blocchi:  100 s -> 2.00 s/blocco
  ultimi  200 blocchi:  400 s -> 2.00 s/blocco
  ultimi 1000 blocchi: 2000 s -> 2.00 s/blocco
Rete a cadenza nominale ADESSO. Proprio per questo il design non deve
dipenderne: una notte di rete scarica allungherebbe 30 blocchi da 60 s a
~10 minuti e la demo diventerebbe inguardabile.

CONVERSIONE (richiesta esplicitamente):
  blocchi          = ceil(expiration_seconds / 2)
  expiresAt(block) = landingBlock + blocchi

  demo    60 s -> 30 blocchi
  1 g  86400 s -> 43.200
  30 g          -> 1.296.000
  3650 g        -> 157.680.000

SI USA ExpirationTime.fromBlocks(), l'unica forma senza conversione. Le
helper wall-clock restano inutilizzate. expiresAt viene RILETTO da
createEntity() e registrato come verità, mai la nostra stima.

Scelto il pattern "lapse" e non lo sliding lease: un record letto spesso non
deve sopravvivere alla propria retention, altrimenti si ribalta l'argomento
GDPR. Documentato anche che un'estensione IMPOSTA una nuova scadenza da ora,
non somma, e che il motore rifiuta un'estensione che non sposti più avanti.


C3. check_schema: 6 warning -> 2
--------------------------------------------------------------------------------
Corretti: sezione entity type, sezione "off Arkiv", token dei predicati non
supportati, Lifetime Extension.

I 2 rimasti NON sono lacune di contenuto: entityTypeHeadings e il gate
vocabolario non scattano nemmeno su un documento minimo che contiene
esattamente ciò che chiedono. Verificato con documenti di 5 righe.
Registrato come F-08.

Predicati esportati dall'SDK ma RIFIUTATI dal nodo (-32002): not-equal,
presence-test, type-test. Negazione via not(eq(...)), che però è il
complemento completo e include le entità che l'attributo non l'hanno mai
avuto: per questo ogni query mantiene un'ancora positiva.


C4. STEP 2b — "Why Arkiv?" nel README (30% del punteggio)
--------------------------------------------------------------------------------
Tesi: "un notaio di cui ti devi fidare non è un notaio". Su Postgres
l'operatore può cancellare una riga, cambiare un timestamp o inserirne una
retrodatata, e nulla nel database lo impedisce o lo registra: il cliente non
si affida alla crittografia ma alla buona condotta di Ancorhash, cioè
esattamente la dipendenza che il prodotto dichiara di rimuovere.

Su Arkiv l'indice stesso è prova: ogni entità è una transazione firmata,
$creator è immutabile e non falsificabile, e un verificatore controlla senza
la nostra API e senza il nostro permesso.

Scadenza nativa = minimizzazione GDPR espressa dal dato, non da una retention
policy che nessuno può verificare.

Separazione privacy: cifrato su Swarm, solo impegni sull'indice pubblico.

PARAGRAFO ONESTO incluso: un database normale farebbe meglio quasi tutto
(più veloce, più economico, join, full-text, niente block time). Arkiv si
guadagna il posto su UN SOLO asse: un verificatore avversariale che non si
fida dell'operatore. Tolto quel lettore, l'argomento crolla.
E: org è DICHIARATO, non dimostrato dalla firma.


C5. FILE TOCCATI
--------------------------------------------------------------------------------
NUOVO  arkiv/schema.md      design completo, 8 sezioni
MOD    README.md            sezione "Why Arkiv?" (~60 righe)
MOD    arkiv/friction.md    +F-08, F-07 precisata (esiste un SDK Python)
MOD    a.txt                questa appendice

Nessun pacchetto installato. Nessun codice di integrazione scritto:
lo STEP 3 attende la review.


================================================================================
APPENDICE D · FASE 2 — BACKEND & INTEGRAZIONE ARKIV
Data: 2026-09-11
================================================================================

D0. UNA ISTRUZIONE NON ESEGUITA COME SCRITTA  [decisione dell'utente]
--------------------------------------------------------------------------------
La richiesta diceva: "The remaining 64 chars of the SwarmReference (the KMS
decryption key) must be placed inside the unindexed JSON payload."

NON eseguita. Su Arkiv "non indicizzato" NON vuol dire "privato": il payload è
un campo selezionabile, restituito a chiunque.

  official/typescript-sdk/querying-data:
    payload | Uint8Array | The raw payload bytes
    "Call select() with no argument, or with "*", to fetch all eleven fields."
  manual/prizes.md:44:
    "It is not a confidentiality layer. Entities are public and verifiable by
     design, so secrets and personal data stay out."

Conseguenza: chiunque esegua la query Q1 di schema.md avrebbe letto
swarm_address dagli attributi e la chiave dal payload, ricomposto la reference
di 128 hex e decifrato il documento. Per ogni documento, in modo permanente e
irreversibile.

L'utente ha scelto "Keep the key out of Arkiv". La chiave resta nel browser,
esattamente come in Fase 1.


D1. STEP 1 — DTO E DOMINIO C#
--------------------------------------------------------------------------------
MOD NotarizeHttpRequest.cs   + swarm_reference (string), + expiration_seconds (int)
MOD NotarizationCommand.cs   + SwarmReference, + ExpirationSeconds
MOD NotarizeFunction.cs      mappatura dei due campi + catch ArkivIndexingException
MOD NotarizationService.cs   validazione

VALIDAZIONE (difesa in profondità sull'invariante):
  - 128 hex  -> RIFIUTATO con log di errore esplicito. Se arriva una reference
                completa, il frontend ha smesso di chiamare toPublicAddress()
                e sta per pubblicare la chiave.
  - 64 hex   -> richiesto, senza prefisso 0x
  - expiration_seconds in [60, 315.360.000]  (1 minuto .. 3650 giorni)
    Il tetto coincide esattamente con MAX_VALIDITY_DAYS della UI.

MOD NotarizationResult.cs / NotarizeHttpResponse.cs
  + arkiv_entity_key, + arkiv_expires_at_block (stringa: è un uint64)


D2. STEP 2 — WRITER NODE
--------------------------------------------------------------------------------
NUOVO src/Ancorhash.Infrastructure/ArkivWriter/
      package.json, .env.example, .gitignore, src/write-entity.mjs
      npm install @arkiv-network/sdk@0.8.0 viem  -> 15 pacchetti, 0 vulnerabilità

ESM puro, nessuno step di build: un bridge invocato da Process non deve poter
fallire in compilazione.

API VERIFICATA SUI TYPINGS INSTALLATI, non sulla documentazione:
  bytes32(value: Hex | Uint8Array)         -> serve il prefisso 0x
  u64(value: bigint | number | string)
  str(value: string)                        rifiuta i control char
  i32(value: number | bigint)
  ExpirationTime.fromBlocks(blocks: number) -> NUMBER, non bigint
  walletClient.createEntity(params, txParams?)
    -> { entityKey: Hex, txHash: Hash, expiresAt: bigint }
  jsonToPayload(json: object): Uint8Array

SCHEMA APPLICATO (arkiv/schema.md C1), 9 attributi:
  app str("ancorhash") · type str("notarization") · schema_version i32(1)
  document_hash bytes32 · swarm_address bytes32 · org str · doc_type str
  chain_id u64 · notarized_at u64(Date.now())

PAYLOAD: solo metadati di presentazione. NESSUNA CHIAVE.

MISSION 02:
  lifetimeBlocks = ceil(expiration_seconds / 2)
  ExpirationTime.fromBlocks(lifetimeBlocks)     60 s -> 30 blocchi
  expiresAt RILETTO dalla ricevuta, mai stimato.

  Si usa fromBlocks e NON atBlock(head + n): a.txt C2 documenta che una
  scadenza assoluta calcolata su un head già vecchio può atterrare dopo ed
  essere rifiutata come "dead on arrival". Una durata si risolve
  all'atterraggio e non può correre.

SICUREZZA DEL WRITER:
  - input su STDIN, non su argv: gli argomenti sono visibili nella process list
  - assertNoFullReference(): se un QUALSIASI campo contiene 128 hex, aborto
    prima di toccare la rete
  - chiave privata solo da ARKIV_PRIVATE_KEY, mai loggata, mai in output
  - in errore non si restituisce né stack né echo dell'input


D3. STEP 3 — PONTE C# -> NODE
--------------------------------------------------------------------------------
NUOVO IArkivIndexer.cs, ArkivIndexResult.cs, ArkivIndexingException.cs,
      ArkivOptions.cs, NodeArkivIndexer.cs

System.Diagnostics.Process, STDIN/STDOUT JSON, timeout configurabile (90 s),
kill dell'intero albero di processi su timeout. La chiave privata passa
dall'ambiente del figlio e segue lo stesso pattern del relayer EVM
(Key Vault in cloud, variabile d'ambiente in locale).

ORDINE: Arkiv PRIMA del relayer, come richiesto. Se l'indicizzazione fallisce
non si è ancora speso gas. Un'entità Arkiv orfana, se il broadcast fallisse
dopo, scade da sola: è esattamente il comportamento voluto.

CONSEGUENZA DA REGISTRARE: girando prima del relayer, tx_hash NON esiste
ancora e quindi NON finisce nel payload, che schema.md §3 invece prevede.
Per averlo servirebbe un Patch (tag 2) successivo, oppure invertire l'ordine
e rinunciare al risparmio di gas. Non risolto: è una scelta aperta.


D4. VERIFICA ESEGUITA
--------------------------------------------------------------------------------
  dotnet build            0 errori, 0 avvisi
  npx tsc -b (frontend)   exit 0
  npm run build           OK
  writer, guardia chiave  {"ok":false,"code":"secret_leak_blocked"}  su 128 hex
  writer, chiave assente  {"ok":false,"code":"missing_key"}
  writer, giro completo   costruisce TUTTI gli attributi, la scadenza e arriva
                          a inviare la transazione. Fallisce con
                          "Execution error without revert data" perché il
                          wallet usa-e-getta non ha fondi. guides/known-issues
                          documenta proprio questo errore opaco per i wallet
                          non finanziati: il writer ora aggiunge l'indizio e il
                          link al faucet invece di farlo ridiagnosticare.
  contratto bridge        JSON in forma SnakeCaseLower accettato dal writer,
                          ok/code/error mappano su ArkivWriterOutput

NON VERIFICATO: la creazione reale di un'entità. Serve un wallet finanziato
con test GLM dal faucet https://hub.arkiv.network/faucet, che richiede login e
CAPTCHA: è un passaggio umano, non automatizzabile.


D5. FRICTION
--------------------------------------------------------------------------------
+ F-09  ExpirationTime.fromSeconds() lancia InvalidExpiryError se il valore
        non è un multiplo del block time. La pagina di documentazione presenta
        i helper come una scala continua "from fromSeconds() up to fromYears()"
        senza accennare al vincolo, che compare solo nel JSDoc. fromSeconds(61)
        esplode. Non ci ha bloccati solo perché eravamo già passati a
        fromBlocks() dopo il warning di check_schema.

schema.md: tutti i punti "unverified" ora risolti contro i typings installati.
Stato passato da "design" a "implementato, entità non ancora creata".


D6. FILE TOCCATI
--------------------------------------------------------------------------------
NUOVO  ArkivWriter/{package.json,.env.example,.gitignore,src/write-entity.mjs}
NUOVO  Core/Abstractions/IArkivIndexer.cs
NUOVO  Core/Models/ArkivIndexResult.cs
NUOVO  Core/Exceptions/ArkivIndexingException.cs
NUOVO  Infrastructure/Configuration/ArkivOptions.cs
NUOVO  Infrastructure/Arkiv/NodeArkivIndexer.cs
MOD    NotarizeHttpRequest.cs, NotarizeHttpResponse.cs, NotarizeFunction.cs
MOD    NotarizationCommand.cs, NotarizationResult.cs, NotarizationService.cs
MOD    Infrastructure/DependencyInjection.cs
MOD    arkiv/schema.md, arkiv/friction.md, a.txt


D7. PROSSIMO PASSO
--------------------------------------------------------------------------------
1. UMANO: finanziare il wallet Arkiv dal faucet, poi mettere la chiave in
   ARKIV_PRIVATE_KEY (locale) o Arkiv:PrivateKey (Key Vault).
2. Prima entità reale + prova Mission 02: stessa query prima e dopo la
   scadenza, nessuna chiamata di Delete, polling e non sleep.
3. Decidere la questione tx_hash nel payload (vedi D3).


================================================================================
APPENDICE E · D3 RISOLTO + FASE 3 (AVALANCHE FUJI)
Data: 2026-09-11
================================================================================

E1. INVERSIONE DELL'ORDINE (chiude il punto aperto D3)
--------------------------------------------------------------------------------
PRIMA:  Arkiv -> relayer     (nessun gas speso se l'indice fallisce, ma
                              tx_hash non esisteva ancora e restava fuori dal
                              payload, contro schema.md §3)
ORA:    relayer -> Arkiv     (il token RWA è la fonte di verità; il tx_hash
                              esiste e finisce nel payload non indicizzato)

CONSEGUENZA NUOVA, GESTITA ESPLICITAMENTE: adesso, quando si arriva ad Arkiv,
il gas è GIÀ speso e l'ancora on-chain esiste. Se l'indicizzazione fallisce,
la notarizzazione NON è fallita.

Restituire un errore a chi ha già pagato e possiede una prova valida on-chain
sarebbe disonesto. Quindi NotarizationService cattura ArkivIndexingException
DOPO il broadcast e restituisce comunque un successo:

    arkiv_indexed = false
    arkiv_entity_key = ""
    tx_hash = <valido>

con un LogError. L'asimmetria è voluta: un indice si ricostruisce senza
toccare la blockchain, una transazione no.

ArkivIndexingException resta gestita in NotarizeFunction con 502 SOLO per i
fallimenti che avvengono prima del broadcast (oggi nessuno, dopo l'inversione);
il percorso normale non la propaga più.


E2. FASE 3 — AVALANCHE FUJI C-CHAIN
--------------------------------------------------------------------------------
SCOPERTA UTILE: NethereumBlockchainRelayer era GIÀ chain-agnostic. Le reti
arrivano da Blockchain:Networks indicizzate per chain id, quindi passare a
Fuji è quasi tutto configurazione, non codice. Nessuna riscrittura del relayer.

ENDPOINT VERIFICATO DAL VIVO (non assunto):
  eth_chainId    0xa869 -> 43113          corrisponde
  eth_blockNumber            58.330.628
  eth_gasPrice               160 wei (0,00 gwei)
  RPC            https://api.avax-test.network/ext/bc/C/rpc

CONFIGURAZIONE (local.settings.json, gitignorato):
  Blockchain__Networks__43113__RpcUrl        https://api.avax-test.network/ext/bc/C/rpc
  Blockchain__Networks__43113__Name          Avalanche Fuji C-Chain
  Blockchain__Networks__43113__NativeCoinId  avalanche-2
  Blockchain__Networks__43113__IsTestnet     true
  Blockchain__Networks__43113__ExplorerTxUrl https://testnet.snowtrace.io/tx/

RIMOSSE Sepolia (11155111), Amoy (80002) e Polygon Mainnet (137).
Le prime due su richiesta; la mainnet perché Team1 impone testnet-only e una
mainnet configurata durante una demo è solo un rischio di spesa reale.
Nota: l'RPC di Amoy non risolve nemmeno più come host — era già morto.

NUOVO CAMPO NetworkConfig.ExplorerTxUrl: il link di verifica non era
configurabile, viveva solo come mappa hardcoded nel frontend. Ora il backend
può comporlo e infilarlo nel payload Arkiv.

NUOVO local.settings.json.example, COMMITTATO e senza segreti: i giudici
devono poter riprodurre la configurazione. Il file reale resta gitignorato.

>>> BLOCCANTE PER L'ESECUZIONE REALE <<<
Il wallet del relayer 0x8458cF7ED1f5CeA1fe2cD1aa34E67A53a88Bf38e ha:
    Fuji     0.000000 AVAX     <-- DA FINANZIARE
    Sepolia  1.288919 ETH
Faucet Core: https://core.app/tools/testnet-faucet/ (passaggio umano).
Finché resta a zero, ogni notarizzazione su Fuji fallisce al broadcast.


E3. WRITER NODE — tx_hash NEL PAYLOAD
--------------------------------------------------------------------------------
write-entity.mjs accettava già tx_hash, anchor_chain ed explorer_url: mancava
il lato C# che li inviasse. Ora NodeArkivIndexer:
  - inietta anchorTransactionHash (nuovo parametro di IArkivIndexer)
  - risolve nome chain ed explorer da BlockchainOptions
  - compone explorer_url = ExplorerTxUrl + tx_hash

Payload effettivo verificato in dry run:
  { "tx_hash": "0xabc123",
    "anchor_chain": "Avalanche Fuji C-Chain",
    "explorer_url": "https://testnet.snowtrace.io/tx/0xabc123" }

schema.md §3 ora corrisponde all'implementazione. NESSUNA chiave: la guardia
anti-128-hex continua a controllare ogni campo in ingresso.


E4. AVVERTENZA SU TEAM1 TRACK B — NON ANCORA SODDISFATTA
--------------------------------------------------------------------------------
Come da richiesta, "for now" il relayer manda su Fuji la stessa transazione
dati a 0 valore con l'hash nel campo data. Funziona e produce un tx_hash
verificabile su Snowtrace, ma NON è ancora un RWA tokenizzato.

manual/prizes.md:242 chiede per il Track B:
  "an asset rule, an eligibility or transfer policy, and settlement or
   payment logic"
e dice esplicitamente che "a landing page or a dashboard without an
end-to-end onchain flow will not qualify".

Una transazione dati non ha regola d'asset, non ha policy di trasferimento e
non ha logica di settlement. Per qualificare servirebbe un contratto su Fuji
con almeno: emissione legata al document_hash, una regola di eleggibilità o
di trasferimento, e un passaggio di settlement. Non è stato inventato nulla:
senza un ABI reale avremmo solo indovinato un selector.


E5. VERIFICA ESEGUITA
--------------------------------------------------------------------------------
  dotnet build              0 errori, 0 avvisi
  frontend tsc              exit 0
  frontend oxlint           nessun finding
  frontend vite build       OK
  RPC Fuji                  chainId/head/gasPrice verificati dal vivo
  saldo relayer su Fuji     0 AVAX (bloccante, documentato)
  writer con tx_hash        payload corretto, 30 blocchi di vita, nessun
                            campo a 128 hex
  local.settings.json.example   nessun segreto (grep sulla chiave: 0 match)

NON VERIFICATO end-to-end: serve il wallet Fuji finanziato E il wallet Arkiv
finanziato. Due faucet, entrambi con passaggio umano.


E6. FILE TOCCATI
--------------------------------------------------------------------------------
MOD    Core/Services/NotarizationService.cs   ordine invertito + degradazione
MOD    Core/Abstractions/IArkivIndexer.cs     + anchorTransactionHash
MOD    Core/Models/NotarizationResult.cs      + ArkivIndexed
MOD    Infrastructure/Arkiv/NodeArkivIndexer.cs          inoltra tx/chain/explorer
MOD    Infrastructure/Configuration/BlockchainOptions.cs + ExplorerTxUrl
MOD    Api/Contracts/NotarizeHttpResponse.cs   + arkiv_indexed
MOD    Api/local.settings.json                 sola Fuji (gitignorato)
NUOVO  Api/local.settings.json.example         committato, senza segreti
MOD    Web/src/App.tsx                         explorer Fuji 43113


E7. PROSSIMO PASSO
--------------------------------------------------------------------------------
1. UMANO: finanziare 0x8458cF7ED1f5CeA1fe2cD1aa34E67A53a88Bf38e su Fuji
   (https://core.app/tools/testnet-faucet/) e il wallet Arkiv su
   https://hub.arkiv.network/faucet.
2. Primo giro end-to-end reale: Swarm -> Fuji -> Arkiv.
3. Prova Mission 02: stessa query prima e dopo la scadenza, nessun Delete,
   polling e non sleep, cadenza dei blocchi rimisurata poco prima.
4. Decidere se affrontare davvero il Track B di Team1 (vedi E4) o puntare
   solo su Arkiv e Swarm.


================================================================================
APPENDICE F · FASE 3 REALE — RWA TOKENIZZATO SU AVALANCHE FUJI (Team1 Track B)
Data: 2026-09-12
================================================================================

NOTA: la richiesta citava "a_3.txt". Quel file non esiste; la sezione E4 è in
a.txt ed è stata letta da lì.


F1. IL CONTRATTO — src/Ancorhash.Contracts/src/ConfidentialRWA.sol
--------------------------------------------------------------------------------
ERC721 su OpenZeppelin v5.1.0, Solidity 0.8.28, progetto Foundry.
Le tre cose che manual/prizes.md:242 pretende per il Track B, in ordine:

ASSET RULE
  mintNotarization(bytes32 documentHash, bytes32 swarmAddress) -> uint256
  tokenId == uint256(documentHash). L'unicità non è un controllo che qualcuno
  può dimenticare di scrivere: è una proprietà del tipo. Il secondo mint dello
  stesso documento rientra da solo, dentro _mint di OZ.
  Stato: swarmAddressOf[tokenId], notarizedAtBlock[tokenId].
  Il token è emesso a msg.sender, cioè alla banca / relayer.

TRANSFER POLICY
  mapping(address => bool) public kycApproved, scritta solo da setKycApproval
  (onlyOwner). Applicata dentro _update, che in OZ v5 è il punto unico da cui
  passa OGNI movimento: non esiste un percorso che la aggiri, né transferFrom
  né safeTransferFrom. Mint (from==0) e burn (to==0) restano liberi.

SETTLEMENT
  settlePurchase(uint256 tokenId) external payable, prezzo fisso 0.01 ether.
  Delivery-versus-Payment atomico: verifica il prezzo, verifica il KYC del
  compratore, sposta il token, POI invia i fondi al venditore.
  Ordine checks-effects-interactions + nonReentrant, perché il venditore può
  essere un contratto. Se il venditore rifiuta i fondi, l'intera transazione
  rientra: né consegna né pagamento.


F2. PROVATO, NON SOLO SCRITTO
--------------------------------------------------------------------------------
forge build    38 file, Solc 0.8.28, successo
forge test     14 test, 14 PASS, 0 FAIL

  asset rule      tokenId derivato dall'hash · duplicato rientra ·
                  documenti distinti -> token distinti · input a zero rifiutati
  transfer policy destinatario non-KYC rientra · anche via safeTransferFrom ·
                  solo l'owner approva · la revoca blocca i trasferimenti
  settlement      DvP sposta token e fondi insieme · prezzo sbagliato rientra ·
                  compratore non-KYC rientra · non si compra da sé ·
                  ATOMICITÀ: venditore ostile che rifiuta i fondi -> il token
                  non si muove e il compratore non paga

GIRO COMPLETO SU CATENA VERA (anvil locale), con lo script di deploy:
  deploy      ConfidentialRWA -> 0x5FbDB2315678afecb367f032d93F642f64180aa3
  mint        status 1, owner corretto, swarmAddressOf corretto
  duplicato   revert 0x73c6ac6e
  non-KYC     revert RecipientNotKycApproved
  settlement  status 1, token al compratore, venditore +0.01 ETH esatti

Questo prova anche lo script di deploy, senza spendere un centesimo su Fuji.


F3. C# — CHIAMATA TIPIZZATA, NON PIÙ TRANSAZIONE DATI
--------------------------------------------------------------------------------
NUOVO  Blockchain/Contracts/MintNotarizationFunction.cs
       [Function("mintNotarization","uint256")] con due [Parameter("bytes32")].
       La firma NON è inventata: viene dall'ABI prodotto da forge
       (out/ConfidentialRWA.sol/ConfidentialRWA.json).
       Selector verificato con cast: 0xc7eecb2b.
       Calldata attesa verificata: 0xc7eecb2b + 32 byte hash + 32 byte swarm.

MOD    NethereumBlockchainRelayer.cs
       SendNotarizationAsync ora accetta anche swarmAddress e:
       - con ContractAddress configurato -> mintNotarization tipizzato,
         SendRequestAndWaitForReceiptAsync, quindi il tx_hash restituito è di
         una transazione GIÀ INCLUSA, non solo accettata in mempool. È ciò che
         serve alla Fase 2 per iniettarlo nel payload Arkiv.
       - senza ContractAddress -> fallback alla vecchia transazione dati, con
         LogWarning esplicito: "nessun token RWA viene emesso".
       HexToBytes32() converte 64 hex -> 32 byte e rifiuta lunghezze diverse.
       receipt.Status == 0 -> trattato come duplicato.

MOD    IBlockchainRelayer.cs, NotarizationService.cs   passano swarmAddress
MOD    BlockchainOptions.cs                            + ContractAddress
NUOVO  DuplicateNotarizationException.cs
MOD    NotarizeFunction.cs                             409 Conflict sul duplicato

GESTIONE DUPLICATI (scelta confermata): il contratto rientra, il backend lo
traduce in 409 con messaggio parlante invece del 500 opaco di prima. Prima del
mint c'è anche una view isNotarized() gratuita, che evita di bruciare gas per
una transazione destinata a fallire; se la view non risponde si procede
comunque e sarà il contratto a rifiutare.
Il selector di rientro 0x73c6ac6e è stato verificato con
  cast sig "ERC721InvalidSender(address)"
e riosservato dal vivo su anvil, non preso a memoria.


F4. COSA MANCA PER ESEGUIRE DAVVERO
--------------------------------------------------------------------------------
Il wallet relayer 0x8458cF7ED1f5CeA1fe2cD1aa34E67A53a88Bf38e ha 0 AVAX su Fuji.
Finché resta a zero non si può né deployare né mintare.

  1. faucet Core:  https://core.app/tools/testnet-faucet/
  2. export FUJI_PRIVATE_KEY=0x...
     cd src/Ancorhash.Contracts
     forge script script/DeployConfidentialRWA.s.sol --rpc-url fuji --broadcast
  3. l'indirizzo stampato va in
     Blockchain__Networks__43113__ContractAddress
  4. serve anche il wallet Arkiv finanziato (hub.arkiv.network/faucet)

Finché ContractAddress è vuoto il relayer NON emette token e ricade sulla
transazione dati: funziona, ma non qualifica per il Track B.


F5. ONESTÀ SU COSA QUESTO È E NON È
--------------------------------------------------------------------------------
- kycApproved è una whitelist gestita dall'owner. È una policy di eleggibilità
  reale e applicata on-chain, non un KYC vero: nessuna verifica d'identità,
  nessun attestatore terzo.
- Il prezzo di settlement è una costante, non un book né un oracolo.
- Il token è emesso al relayer, quindi resta vero quanto già scritto in
  schema.md §6: "org" è DICHIARATO, non dimostrato dalla firma.
- Nessun audit. È codice da hackathon, su testnet, con test che passano.


F6. VERIFICA ESEGUITA
--------------------------------------------------------------------------------
  forge build            successo
  forge test             14/14 PASS
  anvil end-to-end       deploy + mint + duplicato + policy + DvP
  cast sig / calldata    selector e encoding confermati
  dotnet build           0 errori, 0 avvisi
  frontend tsc           exit 0
  out/ e cache/          gitignorati; OZ aggiunto come submodule


F7. FILE TOCCATI
--------------------------------------------------------------------------------
NUOVO  src/Ancorhash.Contracts/{foundry.toml,remappings.txt,.gitignore,foundry.lock}
NUOVO  src/Ancorhash.Contracts/src/ConfidentialRWA.sol
NUOVO  src/Ancorhash.Contracts/test/ConfidentialRWA.t.sol
NUOVO  src/Ancorhash.Contracts/script/DeployConfidentialRWA.s.sol
NUOVO  submodule lib/openzeppelin-contracts @ v5.1.0
NUOVO  src/Ancorhash.Infrastructure/Blockchain/Contracts/MintNotarizationFunction.cs
NUOVO  src/Ancorhash.Core/Exceptions/DuplicateNotarizationException.cs
MOD    NethereumBlockchainRelayer.cs, IBlockchainRelayer.cs,
       NotarizationService.cs, NotarizeFunction.cs, BlockchainOptions.cs,
       local.settings.json(.example)
